{
  "name": "telegram-opencode-bot-hybrid",
  "version": "2.0.0-hybrid.0",
  "description": "Telegram opencode bot — Bun+TS gateway, Python workers, opencode server v2",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "test": "bun test",
    "test:py": "venv/bin/pytest -q",
    "check": "bunx tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "grammy": "^1.33.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
